# 🚀 기능 개선안 v2: 멀티 테마 스위칭, 관리자 파라미터 동적 제어, 대량 동시접속 대응

본 문서의 목적은 개발 AI가 소스 코드를 직접 수정/생성할 수 있도록 구체적인 파일 경로, 데이터 아키텍처, 그리고 비즈니스 로직을 명확히 명시하는 데 있습니다.

> **v2 변경사항**: 실제 코드베이스 대조 리뷰를 반영하여 마이그레이션 절차, k-d tree 2-phase 매칭 구조, 테마별 캐시 디렉토리, 워커 풀 런타임 갱신 방식, Windows 원자적 쓰기, 대기열 예측 공식, 동시 빌드 방지, 캐시 무효화, 롤백 전략을 추가함.

---

## 1. 개요 및 요구사항

- **NASA 디폴트 팩 보존**: 기본 NASA 우주사진 데이터는 영구 보존하며, 새로운 사진 풀(예: 과학관 풍경 등)이 추가되더라도 지워지지 않아야 함.
- **동적 테마 스위칭**: 관리자 패널(`admin.html`)에서 라디오 버튼이나 셀렉트 박스로 테마를 전환하면 서버 재시작 없이 실시간으로 타일 DB와 RAM 캐시가 교체되어야 함.
- **코어 파라미터 제어**: 하드코딩된 알고리즘 변수들을 `config.json` 및 `admin.html`로 이관하여 라이브 제어가 가능하도록 확장함.
- **대량 동시접속 대응**: 학교 단체 관람 등 QR 스캔이 짧은 시간에 몰리는 상황(예: 30명 동시)에서도 대기 시간이 선형적으로 폭증하지 않도록 처리 구조를 개선함.

---

## 2. 파일 및 디렉토리 구조 변경

### 2-1. 현재 상태 (마이그레이션 전제)

`public/raw_tiles/`에는 약 11,000장의 이미지가 **서브폴더 없이 플랫 구조**로 직접 들어있음 (날짜별 파일명). 아래 구조 변경은 이 전제를 기준으로 함.

### 2-2. 목표 구조

```
/public
└── /raw_tiles/
    ├── /default_nasa/     # 영구 보존되는 NASA 우주사진 팩 (기존 파일 이관)
    └── /museum_theme/     # 새로 추가된 과학관 사진 팩

/public
└── /tiles/
    ├── /default_nasa/     # 테마별로 분리된 리사이즈 결과물 (기존 tiles/ 삭제 후 재생성 아님, 병존)
    └── /museum_theme/

/data
├── config.json                          # currentTheme 등 제어 파라미터
└── /themes/
    ├── /default_nasa/
    │   ├── tileDB.json
    │   └── tileIndex.kdtree.json
    └── /museum_theme/
        ├── tileDB.json
        └── tileIndex.kdtree.json
```

- **(신규) `tiles/`도 테마별 하위 폴더로 분리 저장.** 전체 삭제 후 재생성 방식은 빌드 실패 시 활성 테마까지 잃는 위험이 있으므로 채택하지 않음. 디스크 용량이 늘어나는 트레이드오프는 있으나, 롤백/캐시 무효화 관점에서 안전함.
- **(신규) `tileDB.json`/`tileIndex.kdtree.json`은 테마별 폴더에 각각 저장.** `config.json`은 `currentTheme` 문자열만 가리키며, 서버는 기동/전환 시 `data/themes/{currentTheme}/`를 읽어 RAM에 적재. 단일 파일 심볼릭 링크 방식은 Windows 호환성 문제가 있어 채택하지 않음 (아래 5-⑤ 참고).

### 2-3. 마이그레이션 절차 (신규)

기존 서비스 중단 없이 진행하기 위한 순서:

1. `scripts/migrate-to-theme-structure.js` 작성: `raw_tiles/` 플랫 구조의 모든 파일을 `raw_tiles/default_nasa/`로 이동 (복사 후 검증 → 원본 삭제 순서로, 중간 실패 시 원본 보존).
2. 기존 `public/tiles/`, `data/tileDB.json`을 `public/tiles/default_nasa/`, `data/themes/default_nasa/tileDB.json`으로 이동.
3. `build.db.js`를 신규 스키마로 재실행하여 `tileIndex.kdtree.json` 생성 (기존 tileDB는 이미 있으므로 이미지 재처리 없이 인덱스만 추가 생성 가능하도록 스크립트에 `--index-only` 옵션 고려).
4. 마이그레이션 완료 검증 후 서버 재시작 1회로 신규 구조 적용 (이후부터는 무중단 테마 전환 가능).

---

## 3. 데이터 스키마 개편 (`/data/config.json`)

```json
{
  "opacity": 0.8,
  "tileSize": 20,
  "blendMode": "multiply",
  "maxResolution": 1440,
  "currentTheme": "default_nasa",
  "maxTileUsage": 4,
  "banRadius": 2,
  "minRequiredTiles": 3000,
  "workerPoolSize": 4,
  "gridDownscaleThreshold": 10,
  "candidatePoolSize": 150
}
```

- `workerPoolSize`: 고정 워커 풀 크기 (기본값은 `os.cpus().length` 기준으로 서버 기동 시 자동 설정, config에는 오버라이드 값만 저장)
- `gridDownscaleThreshold`: 대기열 길이가 이 값을 초과하면 그리드 해상도를 임시로 축소하는 기준치 (섹션 6-④ 참고)
- **(신규) `candidatePoolSize`**: k-d tree에서 뽑아올 넉넉한 후보 개수. 기존 코드의 `TOP_K=60`보다 커야 하며, ban radius/사용 횟수 필터를 통과한 뒤에도 유효 후보가 남도록 150 정도를 기본값으로 함 (섹션 6-② 참고).

---

## 4. 백엔드 코어 로직 수정 및 신규 API 가이드

### ① `scripts/build.db.js` 수정 (테마별 빌드 + k-d tree 인덱스 생성)

- 기존: `public/raw_tiles` 전체 스캔, **순차 `for` 루프**로 처리.
- 변경: 실행 시 인자(`process.argv[2]`) 또는 `config.json`의 `currentTheme` 값을 읽어 해당 폴더만 타게팅 빌드.
  - 예: `node scripts/build.db.js museum_theme` 실행 시 `/public/raw_tiles/museum_theme/`의 이미지를 읽어 리사이즈 후 `/public/tiles/museum_theme/`에 배치하고 `data/themes/museum_theme/tileDB.json` 생성.
- **(신규) 빌드 완료 시 각 타일의 평균 Lab 색상값을 기준으로 3차원 k-d tree를 구축하여 `data/themes/{theme}/tileIndex.kdtree.json`으로 함께 저장.**
- **(신규) 빌드 완료 후 타일 수가 `minRequiredTiles` 미만이면 경고 로그 + `buildResult.warning` 플래그. Lab 색상값의 표준편차가 낮은 경우(색상 편중 테마) `lowDiversityWarning` 플래그도 함께 반환.**
- **(신규) 빌드는 절대 요청-응답 사이클 안에서 동기 실행하지 않음.** 아래 ②의 비동기 큐 처리 참고.
- **(신규) 빌드 처리를 순차 `for` 루프에서 `Promise.all` 기반 배치 병렬화로 전환.** 11,000장 sharp 리사이즈는 순차 처리 시 상당한 시간이 소요되므로, 배치 크기(예: 동시 20~50개)를 조절해 I/O와 CPU 사용률 균형을 맞춤. 테마 전환이 잦을수록 이 최적화의 효과가 큼.

### ② `src/admin.route.js` 신규 API 추가 및 변경

**GET `/api/admin/themes`** — `/public/raw_tiles/` 내부 디렉토리 목록을 읽어 반환.

**POST `/api/admin/config`**
- `currentTheme` 값은 반드시 `/api/admin/themes` 반환값과 대조하여 화이트리스트 검증 후에만 반영. 미검증 값은 400 응답.
- `config.json` 쓰기는 원자적으로 처리 (섹션 5-⑤ 참고).
- `currentTheme` 변경 시:
  - 아직 빌드되지 않은 테마 → `isBuilding` 플래그 세팅 + 즉시 202 응답 + job id 반환. 빌드는 백그라운드 큐에서 처리.
  - **(신규) 동시 빌드 요청 방지**: `isBuilding` 플래그만으로는 두 관리자가 동시에 서로 다른 테마 빌드를 요청하는 경우를 막지 못함. 빌드 요청은 단일 FIFO job queue(예: 메모리 배열 + 순차 처리, 또는 `p-queue` concurrency:1)로 직렬화하여 한 번에 하나의 빌드만 진행.
  - 이미 빌드된 테마 → `reloadTileDB()`로 RAM 캐시(tileDB + k-d tree) 즉시 재적재, 버전 번호 갱신.
  - **(신규) 타일 이미지 RAM 캐시(`globalTileCache`) 무효화**: 현재 `globalTileCache`는 `tiles/` 디렉토리 기준으로 동작. 테마 전환 시 참조 경로가 `tiles/{newTheme}/`로 바뀌므로, 캐시를 통째로 flush 후 신규 테마 이미지로 재적재하는 로직을 `reloadTileDB()`와 같은 트랜잭션에서 함께 처리.

**GET `/api/admin/build-status?jobId=...`** (신규) — 빌드 진행 상태(`pending`/`building`/`done`/`failed`)와 완료 시 `buildResult` 반환.

- **(신규) 빌드 실패 시 롤백 전략**: 빌드가 `failed` 상태가 되면 `currentTheme`는 자동으로 이전 활성 테마로 되돌리지 않고, **관리자에게 실패 사실과 원인을 명시적으로 표시**하고 이전 테마를 계속 서빙 상태로 유지 (즉, `reloadTileDB()`는 빌드 성공 후에만 트리거되므로 실패해도 서비스는 끊기지 않음 — 자동 rollback이 아니라 애초에 전환이 일어나지 않는 구조로 설계). 관리자가 재시도 또는 다른 테마 선택.

### ③ `src/matcher.worker.js` 수정 (하드코딩 제거 + 동적 config 갱신 + k-d tree 2-phase 매칭)

- 기존: `MAX_USAGE = 4`, ban radius `cy - 2 ~ cy + 2` 고정, 전체 tileDB 브루트포스 거리 정렬 후 `TOP_K=60`에서 필터.
- 변경:

```javascript
const MAX_USAGE = workerData.config.maxTileUsage || 4;
const RAD = workerData.config.banRadius || 2;
const CANDIDATE_POOL = workerData.config.candidatePoolSize || 150;
```

- **(신규·중요) k-d tree는 "가까운 후보 추출"만 대체하며, 기존 필터 체인(사용 횟수 초과 스킵 → ban radius ID 체크 → 반경 내 시각적 유사도 체크)은 그대로 유지.** 즉 2-phase 구조:
  1. k-d tree에서 `candidatePoolSize`(기본 150)개의 최근접 후보를 빠르게 조회
  2. 기존 필터(사용 횟수, ban radius, 유사도)를 후보 풀에 순차 적용하여 최종 선택
  - `candidatePoolSize`는 기존 `TOP_K=60`보다 넉넉하게 설정해야 필터 통과 후 유효 후보가 0이 되는 edge case를 방지함.
- **(신규·중요) `workerData`는 워커 생성 시점에 1회만 고정되므로, 이미 떠 있는 워커는 이후 config 변경을 자동 반영하지 않음.**
  - 관리자가 config를 변경하면 워커 풀 내 모든 워커에 `MessageChannel` 또는 `broadcastChannel`로 새 config(및 새 tileDB 참조)를 전달하고, 워커 쪽에서 `parentPort.on('message', ...)`로 런타임에 로컬 변수를 갱신.
  - **(신규) `piscina` 채택 시 유의**: `piscina`는 `workerData`를 워커 생성 시 1회만 전달하는 라이브러리이므로, 런타임 config/tileDB 갱신은 라이브러리 기본 기능이 아니라 별도의 `MessageChannel` 통신 계층을 직접 구현해야 함. 구현 복잡도를 과소평가하지 말고 별도 작업 항목으로 산정.

### ④ `src/mosaic.queue.js` — 요청별 워커 생성 → 고정 워커 풀 전환

- 기존: 요청마다 `new Worker()` 생성.
- 변경: `config.json`의 `workerPoolSize`(기본값 `os.cpus().length`) 크기의 고정 워커 풀 구성. 요청은 큐에 쌓여 풀이 처리.
- **(신규) `globalTileDB`(약 471KB JSON, 11,000개 엔트리) 전달 비용**: 현재 매 요청마다 `workerData`로 전체 tileDB를 넘기고 있음(`upload.route.js`). 고정 풀 전환 시 초기 로딩 1회로 해결되나, 테마 전환 시에는 풀 내 모든 워커에 재전달 필요.
  - **(선택·향후 최적화) `SharedArrayBuffer`에 Lab 색상값을 packed float array로 저장**하면 워커 간 복사 없이 공유 가능해 직렬화 비용을 제거할 수 있음. 다만 Lab 데이터 구조를 배열 인덱스 기반으로 재설계해야 하므로, 1차 구현에서는 MessageChannel 방식으로 가고 성능이 부족할 때 2차 최적화로 고려.

---

## 5. 관리자 UI 개편 (`public/admin.html` 추가 UI 요소)

### ① 테마 스위칭 섹션

```html
<div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200">
  <h2 class="text-lg font-bold mb-4 flex items-center gap-2"><span class="material-symbols-outlined font-bold">folder_open</span> 백그라운드 테마 스위칭</h2>
  <p class="text-sm text-slate-500 mb-4">안전하게 보존된 기본 우주사진과 신규 사진 팩을 클릭 한 번으로 교체합니다.</p>
  <div id="themeRadioGroup" class="space-y-3">
    <!-- API 연동으로 동적 생성되는 영역 -->
  </div>
  <div id="themeBuildStatus" class="mt-3 text-sm hidden">
    <!-- "빌드 중... (예상 소요: n초)" / "완료: N개 타일 인덱싱됨" / "실패: 이전 테마 유지 중" -->
  </div>
</div>
```

### ② 디바이스 제어 파라미터 섹션

```html
<div>
  <label class="flex justify-between mb-2">
    <span class="font-bold text-slate-700">타일당 최대 중복 허용 횟수</span>
    <span id="maxUsageVal" class="text-indigo-600 font-bold">4회</span>
  </label>
  <input type="range" id="maxUsageSlider" min="1" max="20" step="1" class="w-full h-2 bg-slate-200 rounded-lg appearance-none cursor-pointer">
</div>

<div id="tileSafetyAlert" class="mt-4 p-4 rounded-xl text-sm font-medium border hidden">
  <!-- 타일 개수가 minRequiredTiles 미만이거나 lowDiversityWarning일 때 warning 클래스로 노출 -->
</div>
```

### ⑤ `config.json` 원자적 쓰기 — Windows 고려사항 (신규)

- 서버가 Windows 환경에서 구동 중인 경우, `fs.rename`은 대상 파일이 이미 존재할 때 **EPERM 에러**를 발생시킬 수 있음.
- 단순 `write temp → fs.rename` 시퀀스 대신 다음 중 하나 채택:
  - `write-file-atomic` 패키지 사용 (Windows 호환 처리 내장)
  - 직접 구현 시: `write(tempFile) → unlink(oldFile) → rename(tempFile, oldFile)` 순서로 처리하고 rename 실패 시 재시도 로직 포함

---

## 6. 대량 동시접속 대응

### 배경

현재 로그 기준 단일 요청 처리 시간이 4.7~7.2초 수준.

```
[2026. 7. 6. PM 4:04:22] 새로운 모자이크 완성 (소요시간: 4.72s, 해상도: 1920x1080, 고유 타일: 2915종)
[2026. 7. 7. AM 9:50:17] 새로운 모자이크 완성 (소요시간: 7.20s, 해상도: 1600x1920, 고유 타일: 3354종)
```

70×39 그리드(~2,700셀) 기준 셀당 전체 타일 풀(~11,000장) 브루트포스 Lab 거리 비교 시 요청당 약 2,970만 회의 거리 계산이 발생. 학교 단체 관람처럼 QR 스캔이 짧은 시간에 몰리면(예: 30명 동시) 다음 문제로 이어짐:

1. 물리 코어 수를 넘는 동시 워커는 컨텍스트 스위칭만 늘고 순차 처리에 가까워짐.
2. 요청마다 O(n) 브루트포스가 반복되어 대기열 마지막 사용자는 수 분 이상 대기 가능.

### 대응 우선순위

**① (최우선) 브루트포스 매칭을 k-d tree 인덱스 조회로 교체**

- `build.db.js`에서 테마 빌드 시 Lab 색공간(L, a, b) 기준 3차원 k-d tree를 함께 생성.
- 단일 요청 처리 시간을 5초대에서 1초 미만 수준으로 단축하는 것이 목표.

**② k-d tree는 후보 추출만 담당, 기존 필터 체인은 유지 (신규·중요)**

- k-d tree로 대체 가능한 것은 "색상 거리순 가까운 후보 뽑기" 단계뿐이며, 사용 횟수 초과 스킵 / ban radius ID 체크 / 반경 내 시각적 유사도 체크는 k-d tree로 대체되지 않음.
- 2-phase 구조로 구현: k-d tree에서 `candidatePoolSize`(기본 150, 기존 `TOP_K=60`보다 넉넉하게)개의 후보를 먼저 뽑고, 그 위에 기존 필터를 적용.
- 후보 풀을 너무 좁게 잡으면 필터 통과 후 유효 후보가 0이 되는 edge case가 늘어나므로, 최소한 기존 `TOP_K` 이상으로 설정.

**③ 요청별 워커 생성 방식을 고정 워커 풀로 전환**

- `mosaic.queue.js`의 요청별 `new Worker()` 생성을 `workerPoolSize`(기본 `os.cpus().length`) 크기의 고정 풀로 전환.
- 테마 전환 시 풀 내 전체 워커에 config/tileDB를 재전달하는 로직은 `MessageChannel`/`broadcastChannel` 기반으로 별도 구현 (섹션 4-③ 참고, `piscina` 채택 시에도 이 부분은 라이브러리가 자동 처리하지 않음).

**④ In-flight 요청의 tileDB 버전 고정**

- per-request shallow-clone 시점에 tileDB 버전(테마 전환 시 갱신되는 버전 번호)을 함께 스냅샷하여, 처리 도중 관리자가 테마를 전환하더라도 해당 요청은 끝까지 원래 버전을 참조하도록 고정.

**⑤ (선택) 대기열 기반 실시간 상태 표시 및 적응형 그리드 축소**

- 기존 `upload.route.js`의 Socket.io progress emit 로직을 확장하여 "현재 N번째 대기 중, 예상 대기시간 n초" 표시.
- **(신규) 예상 대기시간 산출 공식**: k-d tree 적용 전후로 셀당 처리 시간이 크게 달라지므로 고정 상수 대신, **최근 N건(예: 20건)의 실제 처리 소요시간에 대한 지수이동평균(EMA)** 을 서버가 추적하여 `예상 대기시간 = EMA(최근 처리시간) × (대기열 내 내 앞 순번 수) / workerPoolSize`로 계산.
- 대기열 길이가 `gridDownscaleThreshold`를 초과하면 그리드를 70×39 대신 35×20 등으로 임시 축소, 대기열 해소 시 자동 복귀. ①~④ 적용 후에도 부족할 경우에 한해 추가 검토.

---

## 7. 구현 순서 (의존성 기반, 신규)

기획서의 우선순위를 존중하되, 각 단계가 이전 단계의 결과물에 의존하는 순서로 재배열. 각 단계마다 중간 테스트 가능:

1. **디렉토리 구조 변경 + 마이그레이션 스크립트** (`raw_tiles`, `tiles`, `data/themes` 신규 구조로 이관)
2. **`build.db.js` 테마별 빌드 + k-d tree 인덱스 생성 + `Promise.all` 병렬화**
3. **`config.json` 스키마 확장 + 원자적 쓰기 (`write-file-atomic`)**
4. **`matcher.worker.js`: k-d tree 2-phase 매칭 교체** (후보 추출 → 기존 필터 적용)
5. **`mosaic.queue.js`: 고정 워커 풀 전환 + MessageChannel 기반 런타임 config 갱신**
6. **`admin.route.js`: 테마 API + 비동기 빌드 큐(FIFO, 동시 빌드 방지) + build-status API + 캐시 무효화(`globalTileCache` flush)**
7. **`admin.html` UI 확장** (테마 스위칭, 파라미터 슬라이더, 빌드 상태 표시)
8. **대기열 상태 표시 (EMA 기반 예상 대기시간) + 적응형 그리드 축소** (선택)

---

## 8. 구현 시 유의사항 요약 (개발 AI 체크리스트)

- [ ] `raw_tiles` 플랫 구조 → 테마 폴더 마이그레이션 스크립트 우선 실행 (원본 보존 후 삭제)
- [ ] `tiles/`, `tileDB.json`, `tileIndex.kdtree.json`을 테마별 하위 폴더로 분리 저장 (심볼릭 링크 방식 지양)
- [ ] k-d tree는 후보 추출만 담당 — 사용 횟수/ban radius/유사도 필터 체인은 그대로 유지, `candidatePoolSize`는 기존 `TOP_K` 이상으로 설정
- [ ] 워커는 `workerData` 1회 고정 값이므로, config/tileDB 변경 시 `MessageChannel`/`broadcastChannel` 기반 런타임 갱신 로직 필수 구현 (`piscina` 사용 시에도 별도 구현 필요)
- [ ] 테마 빌드는 반드시 비동기 job 큐(FIFO, 동시 빌드 방지)로 처리, 동기 실행 금지
- [ ] `currentTheme`는 화이트리스트 검증 후에만 반영
- [ ] `config.json`은 `write-file-atomic` 또는 Windows 안전 시퀀스로 원자적 쓰기
- [ ] tileDB 교체 시 버전 번호 갱신, in-flight 요청은 요청 시점 버전으로 고정
- [ ] 테마 전환 시 `globalTileCache`(타일 이미지 RAM 캐시) flush 후 재적재
- [ ] 빌드 실패 시 자동 rollback이 아닌 "전환 미실행 + 이전 테마 계속 서빙 + 관리자 실패 알림" 구조로 설계
- [ ] `build.db.js` 순차 `for` 루프 → `Promise.all` 배치 병렬화
- [ ] 대기열 예상 시간은 EMA 기반으로 산출 (고정 상수 금지)
- [ ] 타일 수 부족 경고 외에 색상 분산 기반 다양성 경고도 함께 노출